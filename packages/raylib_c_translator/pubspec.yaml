name: raylib_c_translator
description: A starting point for Dart libraries or applications.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  c_proto_parser: ^0.0.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
